// DO NOT commit config.js — Cloudflare Pages generates it at build time from MAPS_API_KEY env var.
// For local testing only: copy this to config.js and add your real key.
const MAPS_API_KEY = 'YOUR_GOOGLE_MAPS_API_KEY';
